import Layout from "../../components/Layout"

export default function Navegacao() {
    return (
        <Layout titulo="Exemplo de Navegação #01">
            <h1>Navegação #01</h1>
            {/* <p>Como esse arquivo se chama index, na hora de escrever o caminho para o mesmo, basta escrever o nome da pasta, visto que "/navegacao/" cairá no index da página.</p> */}
        </Layout>
    )
}